import * as React from 'react';

import { Input } from '@/components/ui/input';

const CurrencyInput = () => {
  return <Input type="number" placeholder="Base currency" />;
};

export { CurrencyInput };
